#include <stddef.h>

size_t get_count(const char *s)
{
  int count = 0;
  int i = 0;
  for ( i = 0; s[i] != '\0'; i++) {
    switch(s[i]) {
      case 'a':
      case 'e':
      case 'i':
      case 'o':
      case 'u':
        count++;
        break;
        }
  }
    return count;
}
